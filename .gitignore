# --- 1. ความปลอดภัย (สำคัญที่สุด) ---
.env
.env.*
*.key

# --- 2. Python System Files (ไฟล์ขยะที่ Python สร้างเอง) ---
__pycache__/
*.pyc
*.pyo
*.pyd

# --- 3. Virtual Environment (อย่าเอาขึ้น เพราะเครื่องเพื่อนต้องลงเอง) ---
.venv/
venv/
ENV/

# --- 4. Tools & Linter Cache (Ruff ที่คุณใช้อยู่) ---
.ruff_cache/
.pytest_cache/
.mypy_cache/

# --- 5. Database & Local Storage (ข้อมูล Vector DB หนักมาก ห้ามเอาขึ้น) ---
qdrant/
storage/
snapshots/

# --- 6. IDE & OS Files (ไฟล์ตั้งค่าของเครื่องเราคนเดียว) ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- 7. ไฟล์ชั่วคราวอื่นๆ ---
*.log
temp/
*.pdf
__pycache__
qdrant
uploads
testing.py
